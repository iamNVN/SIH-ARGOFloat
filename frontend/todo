analytics page, show modal dialog, close button doesnt close the dialog. 
netcdf to sql convertor, download and import button dont do anyhting. make it download a sample sql file, and import will show modal saying added to dB with details below.
session is not working at all. reloading shows login page even if logged in. 
map view page, map is not visible
make login page alone centered instead of wide

all the messages are redisplayed after getting repsonse from backend